баг с миграциями:
исправил в конфиге кодировку database.php
            'charset' => 'utf8',
            'collation' => 'utf8_unicode_ci',

https://www.youtube.com/watch?v=R-oH8iTAhVc

обновление миграций и сидов
	php artisan migrate:refresh --seed

при слонирование проекта из .env проекта пропал app.key. Необходимо 
	php artisan key:generate
	php artisan config:clear

просмотр маршрутов
	php artisan route:list

https://packagist.org/packages/filp/whoops

активные ссылки 
	{{Request::path() == 'admin' ? 'active' : ''}}

пакет для отладки страниц
	filp/whoops


если нет данных из контроллера
	
	@if (! empty($country)) 
		{{$country->name}}
	 @else
		{{'Hello world'}}
	@endif



объединение коллекции

	$concatenated = $collection->concat(['Jane Doe'])->concat(['name' => 'Johnny Doe']);